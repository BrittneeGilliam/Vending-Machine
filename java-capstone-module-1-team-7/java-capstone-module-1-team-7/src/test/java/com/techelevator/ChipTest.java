package com.techelevator;

import static org.junit.Assert.assertEquals;

import org.junit.Assert;
import org.junit.Test;

public class ChipTest {

	private Chip bugles = new Chip("Bugles", 1.29);
	
	
public void cigar_party_one() {
		
		assertEquals("Bugles", bugles.getName());
		a(1.29, bugles.getPrice());
		assertEquals(true, exercises.haveParty(70, true));
	}
	
	
	
	
		
	
}
